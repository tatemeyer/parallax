//! The Parallax cockpit. Read-only: it observes every registered
//! project and mutates nothing, anywhere.

fn main() {
    // Wired in Task 15; Arcs 1 and 2 build the view model and its
    // rendering, neither of which needs a terminal.
}
